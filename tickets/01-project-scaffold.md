# T-01 Scaffolding & Core Architecture

Status: completed
Depends on: none

## Work
Scaffold Vite + React + TypeScript in `/Users/neo/dev/resample-studio`. Configure Tailwind CSS, Lucide icons, Three.js, and font styling. Setup basic responsive studio layout with header, visualizer container, knob control panel, playlist sidebar, and export toolbar.

## Done when
Vite development server runs cleanly and displays the dark studio layout skeleton.

## Verification
`pnpm build` succeeds with zero TypeScript or build errors.
